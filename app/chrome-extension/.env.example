# Optional for local unpacked builds that need a deterministic extension ID.
# Chrome's manifest `key` field accepts a public key only. Never put a private
# key in this file. Formal GitHub releases do not require or set this value.
CHROME_EXTENSION_KEY=
